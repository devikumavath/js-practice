// checks given number is postive or negative 

// change num  value and run to see differnt in output 
// example num = -8;        
let num = 8;

if (num>0) {
    console.log(`${num} is postive number`);
} else {
    console.log(`${num} is negative number`);
}