/* upper case to lower case or vise-versa */


/* change variable ' a ' value to see changes 
srting type (lower and upper case)
*/
let a = 'DEVI';
let result = a.toUpperCase();


if ( a !==  result ) {
    console.log(`small letters ${a} is converted into capital letter ${result}`);
 
} else
 {
    let result = a.toLowerCase();
    console.log(`capital letters ${a} is converted into small letter ${result}`);
 
}